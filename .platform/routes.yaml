https://{default}/:
  cache:
    cookies: ["/^SSESS.*/"]
    default_ttl: 2000
    enabled: true
    headers:
    - Accept
    - Accept-Language
  redirects:
    expires: -1s
    paths: {}
  ssi:
    enabled: false
  type: upstream
  upstream: app:http
http://{default}/:
  redirects:
    expires: -1s
    paths: {}
  to: https://{default}/
  type: redirect
https://www.essexlocaloffer.org.uk/:
  redirects:
    expires: -1s
    paths: {}
  to: https://{default}/
  type: redirect
https://essexlocaloffer.org.uk/:
  redirects:
    expires: -1s
    paths: {}
  to: https://{default}/
  type: redirect
