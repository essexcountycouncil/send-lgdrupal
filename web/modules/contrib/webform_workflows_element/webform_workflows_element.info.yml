name: Webform Workflows Element
type: module
description: Provides a webform element that hooks into the core Workflows module to allow transitioning submissions through workflow states.
package: Webform
core_version_requirement: ^9 || ^10
dependencies:
  - webform:webform
  - workflows
  - token:token
